name: Simple CI Practice

on:
  push: 
    branches: [master]

jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: print message
        run: echo "Hi Wellcome to my first github action workflow"
      - name: check node version
        run: node -v
      - name: List of all files
        run: ls -la
      


